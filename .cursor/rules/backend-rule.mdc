---
alwaysApply: true
---

# NextWorld Backend API DTO Reference

이 문서는 NextWorld 백엔드 API의 Request/Response DTO 구조를 정의합니다. 프론트엔드에서 API를 호출할 때 이 구조를 참고하여 타입을 정의하고 사용하세요.

## 공통 응답 형식

모든 API 응답은 `BaseResponse<T>` 형식을 따릅니다.

```typescript
interface BaseResponse<T> {
  success: boolean;      // 요청 성공 여부
  code: number;          // HTTP 상태 코드
  message: string;       // 응답 메시지
  data: T;               // 응답 데이터 (제네릭 타입)
}
```

## 인증 (Authentication)

### SignupRequest

```typescript
interface SignupRequest {
  name: string;                    // 한글 2-10자 (정규식: ^[가-힣]{2,10}$)
  nickname: string;                // 영문+숫자 조합 8자 이상 (정규식: ^(?=.*[a-zA-Z])(?=.*\d)[A-Za-z\d]{8,}$)
  email: string;
  password: string;                 // 8자 이상
  passwordConfirm: string;
}
```

### SignupResponse

```typescript
interface SignupResponse {
  userId: number;
  email: string;
  nickname: string;
}
```

### LoginRequest

```typescript
interface LoginRequest {
  email: string;
  password: string;
}
```

### LoginResponse

```typescript
interface LoginResponse {
  accessToken: string;
  refreshToken: string;
  email: string;
  nickname: string;
}
```

## 사용자 프로필 (User Profile)

### ProfileUpdateRequest

```typescript
interface ProfileUpdateRequest {
  name?: string;
  bio?: string;
  contactEmail?: string;
  twitter?: string;
  profileImage?: File;              // binary (multipart/form-data)
}
```

### UserProfileResponse

```typescript
interface UserProfileResponse {
  userId: number;
  email: string;
  nickname: string;
  pointsBalance: number;
  totalEarned: number;
  guideline: string;
}
```

## 작품 (Work)

### WorkRequestDto

```typescript
interface WorkRequestDto {
  title?: string;
  description?: string;
  coverImageUrl?: string;
  tags?: string[];
  genre?: Genre;
  universeDescription?: string;
  allowDerivative?: boolean;
  guidelineRelation?: string;
  guidelineContent?: string;
  guidelineBackground?: string;
  bannedWords?: string[];
  isPaid?: boolean;
  price?: number;
  allowDerivativeProfit?: boolean;
}

enum Genre {
  ROMANCE = "ROMANCE",
  FANTASY = "FANTASY",
  THRILLER = "THRILLER",
  MARTIAL_ARTS = "MARTIAL_ARTS",
  DRAMA = "DRAMA",
  SF = "SF",
  COMEDY = "COMEDY"
}
```

### WorkResponseDto

```typescript
interface WorkResponseDto {
  id: number;
  title: string;
  description: string;
  coverImageUrl: string;
  tags: string[];
  universeDescription: string;
  allowDerivative: boolean;
  guidelineRelation: string;
  guidelineContent: string;
  guidelineBackground: string;
  bannedWords: string[];
  isPaid: boolean;
  price: number;
  allowDerivativeProfit: boolean;
  authorName: string;
}
```

## 포스트 (Post)

### PostRequestDto

```typescript
interface PostRequestDto {
  title?: string;
  content?: string;
  workType?: WorkType;
  creationType?: CreationType;
  workId?: number;
  status?: PostStatus;
  parentId?: number;
}

enum WorkType {
  SHORT = "SHORT",
  SERIALIZED = "SERIALIZED"
}

enum CreationType {
  ORIGINAL = "ORIGINAL",
  DERIVATIVE = "DERIVATIVE"
}

enum PostStatus {
  DRAFT = "DRAFT",
  PUBLISHED = "PUBLISHED"
}
```

### PostResponseDto

```typescript
interface PostResponseDto {
  id: number;
  title: string;
  content: string;
  authorName: string;
  workTitle: string;
  status: PostStatus;
  workType: WorkType;
  creationType: CreationType;
  createdAt: string;               // ISO 8601 date-time
  updatedAt: string;                // ISO 8601 date-time
}
```

## 댓글 (Comment)

### CommentRequest.Create (댓글 생성)

```typescript
interface CommentRequest {
  Create: {
    content: string;                   // 댓글 내용
    parentCommentId?: number | null;  // 부모 댓글 ID (대댓글 작성 시 전달, 일반 댓글은 null)
  };
  Update: {
    content: string;                   // 수정할 댓글 내용
  };
}
```

또는 개별 인터페이스로 정의:

```typescript
interface CreateCommentRequest {
  content: string;                   // 댓글 내용
  parentCommentId?: number | null;  // 부모 댓글 ID (대댓글 작성 시 전달, 일반 댓글은 null)
}

interface UpdateCommentRequest {
  content: string;                   // 수정할 댓글 내용
}
```

### CommentResponse

```typescript
interface CommentResponse {
  id: number;
  postId: number;
  parentCommentId: number | null;
  authorId: number;
  authorName: string;
  authorImageUrl: string;
  content: string;
  createdAt: string;                // ISO 8601 date-time
  updatedAt: string;                // ISO 8601 date-time
}
```

## 좋아요 (Like)

### LikeResponse

```typescript
interface LikeResponse {
  id: number;
  workId: number;
  workName: string;
  createdAt: string;                 // ISO 8601 date-time
}
```

## 스크랩 (Scrap)

### ScrapResponse

```typescript
interface ScrapResponse {
  id: number;
  targetType: string;
  targetId: number;
  title: string;
  createdAt: string;                // ISO 8601 date-time
}
```

## 포인트 및 결제 (Points & Payment)

### ChargeRequest

```typescript
interface ChargeRequest {
  impUid: string;                    // 아임포트 결제 고유 ID
  amount: number;
}
```

### ChargeOptionsResponse

```typescript
interface ChargeOption {
  chargePoint: number;
  price: number;
  expectedBalance: number;
}

interface ChargeOptionsResponse {
  currentPoints: number;
  chargeOptions: ChargeOption[];
  paymentMethods: string[];
}
```

### UseRequest

```typescript
interface UseRequest {
  amount: number;
  derivativeWorkId: number;
  authorId: number;
}
```

### RefundRequest

```typescript
interface RefundRequest {
  impUid: string;
  amount: number;
  reason: string;
}
```

### VerifyRequest

```typescript
interface VerifyRequest {
  impUid: string;
}
```

### PointsResponse

```typescript
interface PointsResponse {
  balance: number;
}
```

### PayItemResponse

```typescript
interface PayItemResponse {
  payId: number;
  amount: number;
  type: PayType;
  status: PayStatus;
  impUid: string;
  createdAt: string;                 // ISO 8601 date-time
}

enum PayType {
  CHARGE = "CHARGE",
  USE = "USE",
  REFUND = "REFUND"
}

enum PayStatus {
  PENDING = "PENDING",
  COMPLETED = "COMPLETED",
  FAILED = "FAILED",
  REFUND_REQUESTED = "REFUND_REQUESTED",
  REFUNDED = "REFUNDED"
}
```

### PaymentHistoryResponse

```typescript
interface PaymentHistoryResponse {
  title: string;
  opponentName: string;
  amount: number;
  date: string;                     // ISO 8601 date-time
}
```

## 수익 분배 (Revenue)

### DistributeRequest

```typescript
interface DistributeRequest {
  payId: number;
  derivativeWorkId: number;
}
```

### RevenueSettleResponse

```typescript
interface RevenueSettleResponse {
  totalSettledAmount: number;
  remainingUnsettled: number;
  newPointsBalance: number;
}
```

### RevenueSettleHistoryResponse

```typescript
interface RevenueSettleHistoryResponse {
  settledAmount: number;
  previousBalance: number;
  newBalance: number;
  settledAt: string;                 // ISO 8601 date-time
}
```

### RevenueSaleItemResponse

```typescript
interface RevenueSaleItemResponse {
  postTitle: string;
  buyerNickname: string;
  amount: number;
  date: string;                     // ISO 8601 date-time
}
```

### RevenueDashboardResponse

```typescript
interface RevenueDashboardResponse {
  totalSalesCount: number;
  totalRevenue: number;
  originalAuthorFee: number;
  platformFee: number;
  netIncome: number;
}
```

## 리스트 응답 (List Response)

### ListResponse

```typescript
interface ListResponse {
  works: WorkResponseDto[];
  posts: PostResponseDto[];
}
```

## 사용 가이드

### 1. 타입 정의

프론트엔드에서 API를 호출할 때 위의 인터페이스를 TypeScript 타입으로 정의하세요.

```typescript
// 예시: src/types/api.ts
export interface BaseResponse<T> {
  success: boolean;
  code: number;
  message: string;
  data: T;
}

export interface WorkResponseDto {
  id: number;
  title: string;
  // ... 나머지 필드
}
```

### 2. API 호출

API 호출 시 요청/응답 타입을 명시하세요.

```typescript
// 예시: src/api/work.ts
import { BaseResponse, WorkResponseDto, WorkRequestDto } from '@/types/api';

export const createWork = async (
  data: WorkRequestDto
): Promise<BaseResponse<WorkResponseDto>> => {
  const response = await fetch('/api/works', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data),
  });
  return response.json();
};
```

### 3. 에러 처리

`BaseResponse`의 `success` 필드를 확인하여 에러를 처리하세요.

```typescript
const result = await createWork(workData);
if (!result.success) {
  throw new Error(result.message);
}
return result.data;
```

### 4. Enum 사용

Enum 값은 문자열 리터럴 타입으로 정의하세요.

```typescript
export enum Genre {
  ROMANCE = "ROMANCE",
  FANTASY = "FANTASY",
  // ...
}

export type GenreType = Genre | "ROMANCE" | "FANTASY" | "THRILLER" | "MARTIAL_ARTS" | "DRAMA" | "SF" | "COMEDY";
```

### 5. 날짜 형식

모든 날짜 필드는 ISO 8601 형식의 문자열입니다 (`date-time`).

```typescript
// 예시: "2024-01-15T10:30:00Z"
const createdAt = new Date(response.data.createdAt);
```

## 주의사항

1. **필수 필드**: Request DTO에서 필수 필드는 `?`가 없습니다. 모든 필드가 optional인 경우도 있으니 API 엔드포인트 문서를 확인하세요.

2. **파일 업로드**: `ProfileUpdateRequest.profileImage`는 `multipart/form-data` 형식으로 전송해야 합니다.

3. **정규식 검증**: `SignupRequest`의 `name`과 `nickname`은 백엔드에서 정규식으로 검증됩니다.

4. **Enum 값**: Enum 값은 정확히 대문자로 정의된 값만 사용 가능합니다.

5. **null vs undefined**: `parentCommentId`와 같은 필드는 `null`을 허용할 수 있습니다. TypeScript에서는 `number | null`로 정의하세요.

## API 엔드포인트 목록

### 인증 (Authentication)

| Method | Endpoint            | 설명             | Request Body    | Headers                         | Path Variables | Query Params | Response                                    |
| ------ | ------------------- | ---------------- | --------------- | ------------------------------- | -------------- | ------------ | ------------------------------------------- |
| POST   | `/api/auth/signup`  | 회원가입         | `SignupRequest` | -                               | -              | -            | `BaseResponse<SignupResponse>`              |
| POST   | `/api/auth/login`   | 로그인           | `LoginRequest`  | -                               | -              | -            | `BaseResponse<LoginResponse>`               |
| POST   | `/api/auth/logout`  | 로그아웃         | -               | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<Void>`                        |
| POST   | `/api/auth/refresh` | 토큰 갱신        | -               | `Refresh-Token: {refreshToken}` | -              | -            | `BaseResponse<String>` (accessToken만 반환) |
| GET    | `/api/auth/me`      | 현재 사용자 정보 | -               | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<UserProfileResponse>`         |

### 사용자 프로필 (User Profile)

| Method | Endpoint              | 설명        | Request Body                                 | Headers                         | Path Variables | Query Params | Response                                                                                   |
| ------ | --------------------- | ----------- | -------------------------------------------- | ------------------------------- | -------------- | ------------ | ------------------------------------------------------------------------------------------ |
| PUT    | `/api/mypage/profile` | 프로필 수정 | `ProfileUpdateRequest` (multipart/form-data) | `Authorization: Bearer {token}` | -              | -            | `ResponseEntity<?>` (BaseResponse 아님, "프로필이 성공적으로 수정되었습니다." 문자열 반환) |

### 작품 (Work)

| Method | Endpoint                  | 설명                | Request Body                                  | Headers                         | Path Variables | Query Params | Response                            |
| ------ | ------------------------- | ------------------- | --------------------------------------------- | ------------------------------- | -------------- | ------------ | ----------------------------------- |
| POST   | `/api/works`              | 작품 생성           | `WorkRequestDto`                              | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<WorkResponseDto>`     |
| POST   | `/api/works/upload-image` | 작품 이미지 업로드  | `FormData` (multipart/form-data, `file` 필드) | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<String>` (이미지 URL) |
| DELETE | `/api/works/{id}`         | 작품 삭제           | -                                             | `Authorization: Bearer {token}` | `id: Long`     | -            | `BaseResponse<Void>`                |
| GET    | `/api/mypage/works`       | 내 작품 리스트 조회 | -                                             | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<WorkResponseDto[]>`   |

### 포스트 (Post)

| Method | Endpoint                         | 설명                        | Request Body     | Headers                         | Path Variables | Query Params | Response                          |
| ------ | -------------------------------- | --------------------------- | ---------------- | ------------------------------- | -------------- | ------------ | --------------------------------- |
| POST   | `/api/works/posts`               | 포스트 생성 (AI 검수 포함)  | `PostRequestDto` | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<PostResponseDto>`   |
| POST   | `/api/works/posts/save`          | 포스트 임시저장             | `PostRequestDto` | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<PostResponseDto>`   |
| PATCH  | `/api/works/posts/continue/{id}` | 임시저장 포스트 이어서 작성 | `PostRequestDto` | `Authorization: Bearer {token}` | `id: Long`     | -            | `BaseResponse<PostResponseDto>`   |
| GET    | `/api/works/posts/drafts/{id}`   | 임시저장 포스트 조회        | -                | `Authorization: Bearer {token}` | `id: Long`     | -            | `BaseResponse<PostResponseDto>`   |
| DELETE | `/api/works/posts/drafts/{id}`   | 임시저장 포스트 삭제        | -                | `Authorization: Bearer {token}` | `id: Long`     | -            | `BaseResponse<Void>`              |
| GET    | `/api/works/posts/drafts/all`    | 모든 임시저장 포스트 조회   | -                | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<PostResponseDto[]>` |
| GET    | `/api/mypage/posts`              | 내 포스트 리스트 조회       | -                | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<PostResponseDto[]>` |

### 댓글 (Comment)

| Method | Endpoint                          | 설명             | Request Body            | Headers                         | Path Variables    | Query Params | Response                          |
| ------ | --------------------------------- | ---------------- | ----------------------- | ------------------------------- | ----------------- | ------------ | --------------------------------- |
| POST   | `/api/posts/{postId}/comments`    | 댓글 생성        | `CommentRequest.Create` | `Authorization: Bearer {token}` | `postId: Long`    | -            | `BaseResponse<CommentResponse>`   |
| GET    | `/api/posts/{postId}/comments`    | 댓글 생성순 조회 | -                       | -                               | `postId: Long`    | -            | `BaseResponse<CommentResponse[]>` |
| PUT    | `/api/posts/comments/{commentId}` | 댓글 수정        | `CommentRequest.Update` | `Authorization: Bearer {token}` | `commentId: Long` | -            | `BaseResponse<CommentResponse>`   |
| DELETE | `/api/posts/comments/{commentId}` | 댓글 삭제        | -                       | `Authorization: Bearer {token}` | `commentId: Long` | -            | `BaseResponse<String>`            |

### 좋아요 (Like)

| Method | Endpoint              | 설명        | Request Body | Headers                         | Path Variables | Query Params | Response                     |
| ------ | --------------------- | ----------- | ------------ | ------------------------------- | -------------- | ------------ | ---------------------------- |
| POST   | `/api/likes/{workId}` | 좋아요 등록 | -            | `Authorization: Bearer {token}` | `workId: Long` | -            | `BaseResponse<LikeResponse>` |
| DELETE | `/api/likes/{workId}` | 좋아요 취소 | -            | `Authorization: Bearer {token}` | `workId: Long` | -            | `BaseResponse<String>`       |

### 스크랩 (Scrap)

| Method | Endpoint                     | 설명                       | Request Body | Headers                         | Path Variables | Query Params | Response                                               |
| ------ | ---------------------------- | -------------------------- | ------------ | ------------------------------- | -------------- | ------------ | ------------------------------------------------------ |
| POST   | `/api/scraps/works/{workId}` | WORK 스크랩 등록           | -            | `Authorization: Bearer {token}` | `workId: Long` | -            | `BaseResponse<ScrapResponse>`                          |
| DELETE | `/api/scraps/works/{workId}` | WORK 스크랩 삭제           | -            | `Authorization: Bearer {token}` | `workId: Long` | -            | `BaseResponse<String>`                                 |
| POST   | `/api/scraps/posts/{postId}` | POST 스크랩 등록           | -            | `Authorization: Bearer {token}` | `postId: Long` | -            | `BaseResponse<ScrapResponse>`                          |
| DELETE | `/api/scraps/posts/{postId}` | POST 스크랩 삭제           | -            | `Authorization: Bearer {token}` | `postId: Long` | -            | `BaseResponse<String>`                                 |
| GET    | `/api/mypage/scraps/works`   | 내 북마크 WORK 리스트 조회 | -            | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<WorkResponseDto[]>` (스크랩한 Work 목록) |
| GET    | `/api/mypage/scraps/posts`   | 내 북마크 POST 리스트 조회 | -            | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<PostResponseDto[]>` (스크랩한 Post 목록) |

### 포인트 및 결제 (Payment)

| Method | Endpoint                      | 설명           | Request Body    | Headers                         | Path Variables | Query Params | Response                                 |
| ------ | ----------------------------- | -------------- | --------------- | ------------------------------- | -------------- | ------------ | ---------------------------------------- |
| GET    | `/api/mypage/points`          | 내 포인트 조회 | -               | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<PointsResponse>`           |
| GET    | `/api/payment/options`        | 충전 옵션 조회 | -               | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<ChargeOptionsResponse>`    |
| POST   | `/api/payment/charge`         | 포인트 충전    | `ChargeRequest` | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<Void>`                     |
| POST   | `/api/payment/verify`         | 결제 검증      | `VerifyRequest` | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<Boolean>`                  |
| POST   | `/api/payment/use`            | 포인트 사용    | `UseRequest`    | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<Void>`                     |
| POST   | `/api/payment/refund`         | 환불 요청      | `RefundRequest` | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<Void>`                     |
| GET    | `/api/payment/history/charge` | 충전 내역 조회 | -               | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<PaymentHistoryResponse[]>` |
| GET    | `/api/payment/history/use`    | 사용 내역 조회 | -               | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<PaymentHistoryResponse[]>` |
| GET    | `/api/mypage/paylist`         | 결제 내역 조회 | -               | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<PayItemResponse[]>`        |

### 수익 분배 (Revenue)

| Method | Endpoint                      | 설명               | Request Body        | Headers                         | Path Variables | Query Params | Response                                       |
| ------ | ----------------------------- | ------------------ | ------------------- | ------------------------------- | -------------- | ------------ | ---------------------------------------------- |
| POST   | `/api/revenue/settle`         | 수익 정산          | -                   | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<RevenueSettleResponse>`          |
| POST   | `/api/revenue/distribute`     | 수익 분배          | `DistributeRequest` | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<Void>`                           |
| GET    | `/api/revenue/settle/history` | 정산 이력 조회     | -                   | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<RevenueSettleHistoryResponse[]>` |
| GET    | `/api/revenue/sales`          | 판매 내역 조회     | -                   | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<RevenueSaleItemResponse[]>`      |
| GET    | `/api/revenue/dashboard`      | 수익 대시보드 조회 | -                   | `Authorization: Bearer {token}` | -              | -            | `BaseResponse<RevenueDashboardResponse>`       |

### 피드 및 검색 (Feed & Search)

| Method | Endpoint           | 설명                  | Request Body | Headers | Path Variables | Query Params             | Response                     |
| ------ | ------------------ | --------------------- | ------------ | ------- | -------------- | ------------------------ | ---------------------------- |
| GET    | `/api/feed/recent` | 최근 피드 조회        | -            | -       | -              | -                        | `BaseResponse<ListResponse>` |
| GET    | `/api/search`      | 작품/포스트 통합 검색 | -            | -       | -              | `keyword: string` (필수) | `BaseResponse<ListResponse>` |

### 관리자 (Admin)

| Method | Endpoint                             | 설명                | Request Body | Headers                                       | Path Variables | Query Params | Response                          |
| ------ | ------------------------------------ | ------------------- | ------------ | --------------------------------------------- | -------------- | ------------ | --------------------------------- |
| GET    | `/api/admin/payments`                | 환불 요청 목록 조회 | -            | `Authorization: Bearer {token}` (관리자 권한) | -              | -            | `BaseResponse<PayItemResponse[]>` |
| PATCH  | `/api/admin/payments/{payId}/refund` | 환불 승인           | -            | `Authorization: Bearer {token}` (관리자 권한) | `payId: Long`  | -            | `BaseResponse<Void>`              |

### 테스트 (Test)

| Method | Endpoint               | 설명                            | Request Body | Headers | Path Variables | Query Params | Response             |
| ------ | ---------------------- | ------------------------------- | ------------ | ------- | -------------- | ------------ | -------------------- |
| GET    | `/api/test/permit-all` | Permit-All 테스트 (인증 불필요) | -            | -       | -              | -            | `BaseResponse<Void>` |

## API 호출 예시

### 인증이 필요한 API

대부분의 API는 인증이 필요합니다. 인증 토큰은 `Authorization` 헤더에 포함해야 합니다.

```typescript
// 예시: 인증이 필요한 API 호출
const response = await fetch('/api/works', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${accessToken}`,
  },
  body: JSON.stringify(workData),
});
```

### 파일 업로드 API

이미지 업로드 등 파일이 포함된 요청은 `multipart/form-data` 형식을 사용합니다.

```typescript
// 예시: 이미지 업로드
const formData = new FormData();
formData.append('file', file);

const response = await fetch('/api/works/upload-image', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${accessToken}`,
  },
  body: formData,
});
```

### Query Parameters

검색 API 등은 쿼리 파라미터를 사용합니다.

```typescript
// 예시: 검색 API
const queryParams = new URLSearchParams({
  keyword: '검색어', // 필수 파라미터
});

const response = await fetch(`/api/search?${queryParams}`, {
  method: 'GET',
  // 검색 API는 인증 불필요
});
```

### Refresh Token 사용

토큰 갱신 시 `Refresh-Token` 헤더를 사용합니다.

```typescript
// 예시: 토큰 갱신
const response = await fetch('/api/auth/refresh', {
  method: 'POST',
  headers: {
    'Refresh-Token': refreshToken,
  },
});

const result = await response.json();
const newAccessToken = result.data; // 문자열로 accessToken만 반환
```
